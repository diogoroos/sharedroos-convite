/*
class DefineStartRoute {
  final state = context.read<GetLoggedUserImpl>();
    final result = await state.call();
    if (result.email != null) {
      Navigator.pushNamed(context, '/home');
    } else {
      Navigator.pushNamed(context, '/login');
    }
}
*/