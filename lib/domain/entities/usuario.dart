class Usuario {
  final String nomeCompleto;
  final String email;
  final bool isMaster;
  final String token;
  final String id;

  Usuario({this.nomeCompleto, this.email, this.isMaster, this.token, this.id});
}
